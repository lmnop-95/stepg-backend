[project]
name = "stepg-core"
version = "0.0.0"
description = "stepg shared core (config, db, http, errors, features)"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.14",
    "asyncpg>=0.31",
    "pydantic-settings>=2.6",
    "pydantic>=2.10",
    "sqlalchemy[asyncio]>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["stepg_core"]
